# API v1 — Advanced Static Analysis

Scan results include Phase 3 static fields and canonical evidence. `GET /api/v1/static-analysis/rules` exposes rule
metadata and standards mappings. The endpoint intentionally does not expose credentials, sample contents, or proprietary
implementation details.
